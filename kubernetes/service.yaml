---
apiVersion: v1
kind: Service
metadata:
  name: responsive-website-service
  namespace: production
spec:
  type: NodePort
  selector:
    app: responsive-website
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
